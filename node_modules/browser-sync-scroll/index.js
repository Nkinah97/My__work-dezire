/**
 * Main export, can be called when BrowserSync is running.
 * @returns {*}
 */
module.exports = function () {
    if (instance) {
        return emitter.emit(config.PLUGIN_EVENT);
    }
};

/**
 * @param {BrowserSync} bs
 */
module.exports["plugin"] = function (opts, bs) {
	console.log(bs);
}



// (function (bs) {
//
// console.log(bs);
//     // var $elem  = document.querySelector("#wrapper");
//     var socket = bs.socket;
//
//     // Listen to the scroll event on your element
//     // $elem.addEventListener("scroll", function(evt) {
//     //     // Emit the event
//     //     socket.emit("div:scroll", evt.srcElement.scrollTop);
//     // });
//
//     // Listen for the event in other browsers
//     // socket.on("div:scroll", function (scrollTop) {
//     //     // Set the element scroll position to match
//     //     $elem.scrollTop = scrollTop;
//     // });
//
// })(___browserSync___);
